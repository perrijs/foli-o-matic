export interface SelectedWork {
  id: number;
  slug: string;
  name: string;
  color: string;
}

export interface OtherWork {
  name: string;
  url?: string;
}

export interface Award {
  name: string;
  url?: string;
}
